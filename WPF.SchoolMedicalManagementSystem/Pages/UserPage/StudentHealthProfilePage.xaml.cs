using System.Windows.Controls;

namespace WPF.SchoolMedicalManagementSystem.Pages.UserPage
{
    /// <summary>
    /// Interaction logic for StudentHealthProfilePage.xaml
    /// </summary>
    public partial class StudentHealthProfilePage : Page
    {
        public StudentHealthProfilePage()
        {
            InitializeComponent();
            // Khởi tạo ViewModel cho trang này ở đây nếu cần (MVVM)
            // DataContext = new StudentHealthProfileViewModel();
        }
    }
}