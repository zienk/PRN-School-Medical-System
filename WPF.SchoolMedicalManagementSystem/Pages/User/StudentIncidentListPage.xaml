<Page x:Class="WPF.SchoolMedicalManagementSystem.Pages.User.StudentIncidentListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Sự cố Y tế Học sinh">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sự cố Y tế của Học sinh" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" x:Name="dgIncidents" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã sự cố" Binding="{Binding IncidentID}" Width="80"/>
                <DataGridTextColumn Header="Loại sự cố" Binding="{Binding IncidentTypeName}" Width="*"/>
                <DataGridTextColumn Header="Ngày xảy ra" Binding="{Binding IncidentDate, StringFormat='dd/MM/yyyy'}" Width="120"/>
                <DataGridTextColumn Header="Mô tả ngắn" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Mức độ" Binding="{Binding SeverityName}" Width="100"/>
                <DataGridTemplateColumn Header="Thao tác">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Xem chi tiết" Tag="{Binding IncidentID}" Click="ViewIncidentDetails_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>