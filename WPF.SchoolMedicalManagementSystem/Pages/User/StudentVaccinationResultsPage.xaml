<Page x:Class="WPF.SchoolMedicalManagementSystem.Pages.User.StudentVaccinationResultsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Kết quả Tiêm Vaccine">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kết quả Tiêm Vaccine của Học sinh" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" x:Name="dgVaccinationRecords" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Record" Binding="{Binding VaccinationRecordID}" Width="80"/>
                <DataGridTextColumn Header="Tên Vaccine" Binding="{Binding CampaignName}" Width="*"/>
                <DataGridTextColumn Header="Ngày tiêm" Binding="{Binding VaccinationDate, StringFormat='dd/MM/yyyy'}" Width="120"/>
                <DataGridTextColumn Header="Kết quả" Binding="{Binding Result}" Width="150"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="200"/>
                <DataGridTemplateColumn Header="Thao tác">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Xem chi tiết" Tag="{Binding VaccinationRecordID}" Click="ViewVaccinationDetails_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>