<Window x:Class="WPF.SchoolMedicalManagementSystem.NurseView.VaccinationStudentListDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WPF.SchoolMedicalManagementSystem.NurseView"
        Title="Lên danh sách học sinh cho chiến dịch" SizeToContent="Height" Width="1000"
        WindowStartupLocation="CenterScreen" Background="White"
        ResizeMode="CanResize" MinHeight="400" MinWidth="800">
    
    <Window.Resources>
        <!-- ✅ ADDED: Converter để disable checkbox cho học sinh đã thêm -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="LÊN DANH SÁCH HỌC SINH CHO CHIẾN DỊCH" FontSize="20" FontWeight="Bold" Foreground="#0078D4" HorizontalAlignment="Center" Margin="0,0,0,16"/>
        <!-- Bộ lọc -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12" HorizontalAlignment="Left">
            <TextBlock Text="Lọc theo lớp:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="cmbClassFilter" Width="120" Margin="0,0,16,0"/>
            <TextBlock Text="Tên học sinh:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="txtNameFilter" Width="160" Margin="0,0,16,0"/>
            <TextBlock Text="Giới tính:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="cmbGenderFilter" Width="100"/>
            <Button x:Name="btnFilter" Content="Lọc" Margin="16,0,0,0" Width="80" Click="btnFilter_Click"/>
            <Button x:Name="btnClearFilter" Content="Xóa lọc" Margin="8,0,0,0" Width="80" Click="btnClearFilter_Click"/>
        </StackPanel>
        <!-- DataGrid danh sách học sinh -->
        <DataGrid Grid.Row="2" x:Name="dgStudents" AutoGenerateColumns="False" SelectionMode="Extended" SelectionUnit="FullRow" CanUserAddRows="False" Margin="0,0,0,12" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="40">
                    <DataGridTemplateColumn.Header>
                        <CheckBox x:Name="chkSelectAll" Click="ChkSelectAll_Click"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      IsEnabled="{Binding IsAlreadyAdded, Converter={StaticResource InverseBooleanConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Mã HS" Binding="{Binding StudentId}" Width="80"/>
                <DataGridTextColumn Header="Họ và tên" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Lớp" Binding="{Binding Class}" Width="100"/>
                <DataGridTextColumn Header="Ngày sinh" Binding="{Binding DateOfBirth, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Giới tính" Binding="{Binding GenderName}" Width="100"/>
                <DataGridTemplateColumn Header="Trạng thái" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StatusText}" 
                                       HorizontalAlignment="Center"
                                       FontWeight="Medium">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAlreadyAdded}" Value="True">
                                                <Setter Property="Foreground" Value="#28A745"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsAlreadyAdded}" Value="False">
                                                <Setter Property="Foreground" Value="#6C757D"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Nút thao tác -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnSave" Content="Lưu danh sách" Width="140" Height="40" Background="#28A745" Foreground="White" Margin="8" Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Hủy" Width="100" Height="40" Background="#6C757D" Foreground="White" Margin="8" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window> 