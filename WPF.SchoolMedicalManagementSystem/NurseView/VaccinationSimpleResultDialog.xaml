<Window x:Class="WPF.SchoolMedicalManagementSystem.NurseView.VaccinationSimpleResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ghi Nhận Kết Quả Tiêm Chủng" SizeToContent="Height" Width="600"
        WindowStartupLocation="CenterScreen" Background="White"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- Modern Label Style -->
        <Style x:Key="ModernLabel" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,8,0,4"/>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="GHI NHẬN KẾT QUẢ TIÊM CHỦNG" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Foreground="#0078D4" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Form Content -->
        <StackPanel Grid.Row="1">
            <!-- Student Info (Read-only) -->
            <Label Content="Thông tin học sinh:" Style="{StaticResource ModernLabel}"/>
            <TextBox x:Name="txtStudentInfo" 
                     Style="{StaticResource ModernTextBox}"
                     IsReadOnly="True"
                     Background="#F5F5F5"/>

            <!-- Campaign Info (Read-only) -->
            <Label Content="Chiến dịch tiêm chủng:" Style="{StaticResource ModernLabel}"/>
            <TextBox x:Name="txtCampaignInfo" 
                     Style="{StaticResource ModernTextBox}"
                     IsReadOnly="True"
                     Background="#F5F5F5"/>

            <!-- Vaccination Date -->
            <Label Content="Ngày tiêm thực tế:" Style="{StaticResource ModernLabel}"/>
            <DatePicker x:Name="dpVaccinationDate" 
                       Height="40" 
                       Padding="10"
                       Margin="0,0,0,12"/>

            <!-- Result Selection -->
            <Label Content="Kết quả tiêm chủng:" Style="{StaticResource ModernLabel}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <RadioButton x:Name="rbSuccess" 
                           Content="Thành công" 
                           FontSize="14"
                           Margin="0,0,20,0"
                           VerticalAlignment="Center"/>
                <RadioButton x:Name="rbFailed" 
                           Content="Thất bại" 
                           FontSize="14"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Notes -->
            <Label Content="Ghi chú:" Style="{StaticResource ModernLabel}"/>
            <TextBox x:Name="txtNotes" 
                     Style="{StaticResource ModernTextBox}"
                     Height="60"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="Lưu" 
                    Background="#28A745" 
                    Foreground="White"
                    Style="{StaticResource ModernButton}"
                    Click="btnSave_Click"/>
            <Button Content="Hủy" 
                    Background="#6C757D" 
                    Foreground="White"
                    Style="{StaticResource ModernButton}"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>